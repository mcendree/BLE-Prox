package com.example.pixelproximity

// The Wiliot SDK gateway pipeline is no longer used. The app now scans BLE
// directly (RawBleProbe) and resolves Pixel IDs via the Wiliot REST API
// (WiliotRestClient). This file is intentionally left empty.
